plugins {
    id 'base'
}

ext.protobufVersion = "3.6.1"

allprojects {
    apply from: "${project.rootDir}/gradle/properties.gradle"

    group = "io.anemos"
    version = "0.0.5-SNAPSHOT"

    repositories {
        jcenter()
    }
}
